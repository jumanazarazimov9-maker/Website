```html
<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>◈ Erkin Norsafar ◈</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Space+Grotesk:wght@400;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #00ffff;
            --secondary: #ff00ff;
            --accent: #7000ff;
            --dark: #0a0a0f;
        }

        body {
            font-family: 'Space Grotesk', sans-serif;
            background: var(--dark);
            color: var(--primary);
            overflow-x: hidden;
        }

        /* ANIMATED BACKGROUND - ESKI DIZAYN */
        .bg-animation {
            position: fixed;
            inset: 0;
            z-index: 0;
            background: radial-gradient(circle at 20% 50%, rgba(112, 0, 255, 0.15) 0%, transparent 50%),
                        radial-gradient(circle at 80% 50%, rgba(0, 255, 255, 0.15) 0%, transparent 50%);
            animation: bgPulse 10s ease-in-out infinite;
        }

        @keyframes bgPulse {
            0%, 100% { opacity: 0.5; }
            50% { opacity: 1; }
        }

        .stars {
            position: fixed;
            inset: 0;
            z-index: 1;
        }

        .star {
            position: absolute;
            width: 2px;
            height: 2px;
            background: white;
            border-radius: 50%;
            animation: twinkle 3s infinite;
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0.3; transform: scale(1); }
            50% { opacity: 1; transform: scale(1.5); }
        }

        /* NAVIGATION - ESKI DIZAYN, YANGI MATNLAR */
        .navbar {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 1.5rem 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            background: rgba(10, 10, 15, 0.8);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(0, 255, 255, 0.2);
            transition: all 0.3s;
        }

        .logo {
            font-family: 'Orbitron', sans-serif;
            font-size: 1.5rem;
            font-weight: 900;
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            cursor: pointer;
            transition: all 0.3s;
        }

        .nav-menu {
            display: flex;
            gap: 2.5rem;
            list-style: none;
        }

        .nav-link {
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
            position: relative;
            transition: all 0.3s;
            padding: 0.5rem 1rem;
        }

        .nav-link::before {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, var(--primary), var(--secondary));
            transition: all 0.3s;
            transform: translateX(-50%);
        }

        .nav-link:hover::before {
            width: 100%;
        }

        /* HERO SECTION - YANGI MATNLAR */
        section {
            position: relative;
            z-index: 2;
            min-height: 100vh;
            padding: 6rem 5%;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .hero {
            text-align: center;
            padding-top: 80px;
        }

        .hero-title {
            font-family: 'Orbitron', sans-serif;
            font-size: clamp(3rem, 12vw, 10rem);
            font-weight: 900;
            background: linear-gradient(90deg, var(--primary) 0%, var(--secondary) 50%, var(--primary) 100%);
            background-size: 200% auto;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: shimmer 3s linear infinite;
            margin-bottom: 2rem;
            letter-spacing: -0.02em;
            line-height: 1;
            filter: drop-shadow(0 0 40px rgba(0, 255, 255, 0.5));
        }

        @keyframes shimmer {
            to { background-position: 200% center; }
        }

        .hero-subtitle {
            font-size: clamp(1.2rem, 4vw, 2.5rem);
            color: var(--secondary);
            margin-bottom: 3rem;
            animation: glow 2s ease-in-out infinite;
            font-weight: 300;
            letter-spacing: 0.3em;
        }

        .cta-buttons {
            display: flex;
            gap: 2rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 1.2rem 3rem;
            font-size: 1.1rem;
            font-weight: 700;
            text-decoration: none;
            border: 2px solid var(--primary);
            color: var(--primary);
            position: relative;
            overflow: hidden;
            transition: all 0.3s;
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }

        .btn::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            transform: translateX(-100%);
            transition: transform 0.3s;
        }

        .btn:hover::before {
            transform: translateX(0);
        }

        .btn:hover {
            color: var(--dark);
            border-color: var(--secondary);
            box-shadow: 0 0 30px rgba(0, 255, 255, 0.5), 0 0 60px rgba(255, 0, 255, 0.3);
            transform: translateY(-5px);
        }

        .btn span {
            position: relative;
            z-index: 1;
        }

        /* SECTION STYLING */
        .section-title {
            font-family: 'Orbitron', sans-serif;
            font-size: clamp(2.5rem, 6vw, 5rem);
            text-align: center;
            margin-bottom: 1rem;
            background: linear-gradient(90deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* CARD STYLING - YANGI KONTENT UCHUN */
        .card {
            background: rgba(0, 255, 255, 0.03);
            border: 1px solid rgba(0, 255, 255, 0.2);
            padding: 2.5rem;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
            margin-bottom: 2rem;
        }

        .card::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, transparent, rgba(0, 255, 255, 0.1), transparent);
            transform: translateX(-100%);
            transition: transform 0.6s;
        }

        .card:hover::before {
            transform: translateX(100%);
        }

        .card:hover {
            transform: translateY(-15px) scale(1.02);
            background: rgba(0, 255, 255, 0.08);
            border-color: var(--secondary);
            box-shadow: 0 20px 60px rgba(0, 255, 255, 0.2), 0 0 80px rgba(255, 0, 255, 0.1);
        }

        .card-title {
            font-size: 1.5rem;
            color: var(--primary);
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .card-text {
            color: rgba(255, 255, 255, 0.7);
            line-height: 1.8;
        }

        /* RESPONSIVE */
        @media (max-width: 768px) {
            .nav-menu {
                position: fixed;
                top: 80px;
                left: -100%;
                width: 100%;
                background: rgba(10, 10, 15, 0.98);
                flex-direction: column;
                padding: 2rem;
                transition: left 0.3s;
                gap: 1rem;
            }

            .nav-menu.active {
                left: 0;
            }

            .hamburger {
                display: flex;
            }
        }
    </style>
</head>
<body>
    <div class="bg-animation"></div>
    <div class="stars" id="stars"></div>

    <!-- NAVIGATION - YANGI MATNLAR -->
    <nav class="navbar" id="navbar">
        <div class="logo">◈ Erkin Norsafar ◈</div>
        <ul class="nav-menu" id="navMenu">
            <li><a href="#home" class="nav-link">BOSH SAHIFA</a></li>
            <li><a href="#about" class="nav-link">BIZ HAQIMIZDA</a></li>
            <li><a href="#stories" class="nav-link">HIKOYALAR</a></li>
            <li><a href="#plays" class="nav-link">SPEKTAKLLAR</a></li>
            <li><a href="#interviews" class="nav-link">INTERVYULAR</a></li>
            <li><a href="#contact" class="nav-link">ALOQA</a></li>
        </ul>
        <div class="hamburger" id="hamburger">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </nav>

    <!-- HERO - YANGI MATNLAR -->
    <section class="hero" id="home">
        <h1 class="hero-title">ERKIN NORSAFAR</h1>
        <p class="hero-subtitle">◈ YOZUVCHI VA DRAMATURG ◈</p>
        <div class="cta-buttons">
            <a href="#stories" class="btn"><span>HIKOYALAR</span></a>
            <a href="#contact" class="btn"><span>BOG'LANISH</span></a>
        </div>
    </section>

    <!-- HIKOYALAR BO'LIMI -->
    <section id="stories">
        <h2 class="section-title">HIKOYALAR</h2>
        <div class="card">
            <h3 class="card-title">Birinchi Hikoya</h3>
            <p class="card-text">Bu yerda sizning hikoyangizning to'liq matni joylashadi. Hikoyangizni shu yerga yozishingiz mumkin.</p>
        </div>
        <div class="card">
            <h3 class="card-title">Ikkinchi Hikoya</h3>
            <p class="card-text">Ikkinchi hikoyangizning matni bu yerda joylashadi.</p>
        </div>
    </section>

    <!-- SPEKTAKLLAR BO'LIMI -->
    <section id="plays">
        <h2 class="section-title">SPEKTAKLLAR</h2>
        <div class="card">
            <h3 class="card-title">"Spektakl Nomi"</h3>
            <p class="card-text">Spektakl haqida qisqacha ma'lumot. Qaysi teatrda sahnalashtirilgan, qachon va qanday baholangan.</p>
        </div>
    </section>

    <!-- INTERVYULAR BO'LIMI -->
    <section id="interviews">
        <h2 class="section-title">INTERVYULAR</h2>
        <div class="card">
            <h3 class="card-title">Intervyu Sarlavhasi</h3>
            <p class="card-text">Intervyu haqida qisqacha ma'lumot yoki intervyuning to'liq matni.</p>
        </div>
    </section>

    <!-- ALOQA BO'LIMI -->
    <section id="contact">
        <h2 class="section-title">ALOQA</h2>
        <div class="card">
            <h3 class="card-title">Bog'lanish</h3>
            <p class="card-text">Email: erkin@example.com<br>Telegram: @erkin_norsafar</p>
        </div>
    </section>

    <script>
        // JavaScript kodlari bu yerda (oldingi dizayndan)
        // Yulduzlar animatsiyasi va boshqa funksiyalar
    </script>
</body>
</html>
```
